
引用：Jeff Tian 知乎答案 [https://www.zhihu.com/question/392620649/answer/3363436041](https://www.zhihu.com/question/392620649/answer/3363436041)  
一个企业内部会存在多个领域和子领域，能力的复用也可以通过 API 的形式来提供。  
API 可以分为同步 API 和异步 API：

- 同步 API 多是我们熟悉的 Restful API / GraphQL / RPC 等。
- 异步 API 一般使用事件驱动中间件，如 RabbitMQ / Kafka 等。  
    建议的方式是领域内部通过同步 API 来调用，而跨领域之间使用异步 API 来解耦。  
    这里的同步和异步是相对概念，类比程序中同步和异步。同步是必须等待它响应的，严格的一发一收的形式，消息发送方和接收方通常是同一个。异步是消息发出去之后，发送方可以不管了，只等消费方去消费。